// Stub for interactjs — only used at runtime, not needed in unit tests
const interact = () => ({
    draggable: () => ({ on: () => {} }),
    dropzone: () => ({ on: () => {} }),
});
export default interact;
